<?php
$arr = array(
	array(
		 'functions',
		 'Function Demo',
		 'Create three tanks, a numeric input, and four numeric digit indicators in the front panel.  The numeric input will be the control and will take the value of <span class="math"><i>x</i></span>.  The three tanks should be scaled from 0-1, 1-2, and 2-3 respectively.  Label three of the digit indicators for <span class="math">cos(<i>x</i>)</span>, <span class="math">mag(<i>x</i>)</span> and <span class="math">sin(<i>x</i>)</span> and leave the fourth to display the value of <span class="math"><i>x</i></span>',
		 'Moving over to the block diagram, arrange the current modules in an ordered fashion.  Create four boolean expression nodes, one for each numeric indicator.  The first node will connect the input (<span class="math"><i>x</i></span>) with the unnamed indicator.  Edit the value of the node to be <span class="math"><i>x</i></span>.  The node for the cos indicator will be <span class="math">cos(<i>x</i>)</span>.  The node for the mag indicator will be <span class="math">abs(<i>x</i>)</span>.  The node for the sin indicator will be <span class="math">sin(<i>x</i>)</span>.  Finally, connect the three tanks, in parallel, to the input <span class="math"><i>x</i></span>.'
	 )
  );

?>