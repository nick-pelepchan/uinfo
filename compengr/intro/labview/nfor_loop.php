<?php
$arr = array(
	array(
		 'nfor_loop',
		 'Nested <i>for</i> Loop',
		 'Nesting an iteration allows for manipulation of multi-dimensional arrays of data.',
		 'Copying the <i>for</i> loop example given previously, it is nested within another <i>for</i> loop.  The inner loop is set to count every <span class="math"><i>x</i></span> ms, as set by the user.  This loop increments until the maximum value is reached, which is set by the user.  Once this sequence completes, the outer loop increments once and repeats the nests loop within it.  This process is repeated until both loops reach their maximum values and are reset by default.'
	 )
  );

?>